h1 Listing documents

table
  thead
    tr
      th Nombre Documento
      th

  tbody
    - current_user.files.each do |document|
      tr
        td = link_to document.filename, document
        td = button_to 'Eliminar', document_path(document.id), method: :delete, data: { confirm: '¿Estás seguro?' }

br

= link_to 'New Document', new_document_path